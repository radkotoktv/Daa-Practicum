#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

vector<vector<int>> sortTheStudents(vector<vector<int>>& score, int k){
    
}

bool cmp(){

}

int main(){
    vector<int> arr = {6, 4, 2, 9, 0, 10, 3};
    
    for(size_t i = 0; i < 7; ++i){
        cout << arr[i] << " ";
    }

    vector<vector<int>> matrix = {{10, 6, 9, 1}, {7, 5, 11, 2}, {4, 8, 3, 15}};

    for(size_t i = 0; i < 4; ++i){
        for(size_t j = 0; j < 4; ++j){
            cout << matrix[i][j] << " ";
        }
        cout << endl;
    }
    
}